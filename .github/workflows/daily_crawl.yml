name: 每日校園資料更新 (Daily Crawl)

# 設定觸發時機
on:
  # 1. 自動排程：每天 UTC 19:00 (台灣時間凌晨 03:00)
  schedule:
    - cron: '0 19 * * *'
  
  # 2. 手動觸發
  workflow_dispatch:

# ⚠️ 關鍵權限設定
permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests beautifulsoup4 pandas lxml playwright nest_asyncio google-genai pdfplumber
        playwright install chromium

    - name: Run Crawlers and Data Processing
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      run: |
        echo "🕷️ [1/5] 執行靜態爬蟲..."
        python static_crawler_v43_recursive.py
        
        echo "🕷️ [2/5] 執行動態公告爬蟲..."
        python ultimate_bot_builder_v40_printHere.py
        
        echo "🧠 [3/5] 生成 FAQ 題庫..."
        python generate_faq.py
        
        echo "📅 [4/5] 解析行事曆 PDF..."
        python generate_calendar.py
        
        echo "🔄 [5/5] 整合全知資料庫 (Apple Keynote Style Ready)..."
        python merge_data.py

    - name: Commit and Push changes
      run: |
        git config --global user.name "GitHub Action Bot"
        git config --global user.email "actions@github.com"
        
        # 加入所有產出的核心資料
        git add nihs_knowledge_full.json nihs_faq.json nihs_calendar.json || true
        
        timestamp=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
        
        if git commit -m "Auto-update data: ${timestamp} (Early Morning Schedule)"; then
          echo "✅ 提交成功，同步遠端..."
          git pull --rebase origin main
          git push
          echo "🚀 推送完成！台灣時間 03:00 更新任務達成。"
        else
          echo "⚠️ 資料無變動。"
        fi
